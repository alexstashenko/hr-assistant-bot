# 🌐 Деплой HR Assistant Bot — Хостинг 24/7

Это руководство поможет вам развернуть бота на сервере, чтобы он работал круглосуточно и был доступен для всех пользователей.

## 📋 Содержание

1. [Бесплатные варианты](#бесплатные-варианты)
   - [Render.com](#1-rendercom-рекомендуется)
   - [Railway.app](#2-railwayapp)
   - [Fly.io](#3-flyio)
2. [Платные VPS](#платные-vps-опции)
3. [Сравнение вариантов](#сравнение-вариантов)

---

## Бесплатные варианты

### 1. Render.com (Рекомендуется ⭐)

**Преимущества:**
- ✅ Бесплатный план навсегда
- ✅ Простая настройка
- ✅ Автоматический деплой из GitHub
- ✅ Встроенные переменные окружения
- ⚠️ Засыпает после 15 мин неактивности (просыпается за ~30 сек)

#### Шаг 1: Подготовка проекта

Создайте файл `render.yaml` в корне проекта:

```yaml
services:
  - type: web
    name: hr-assistant-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python hr_assistant_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
```

Обновите `hr_assistant_bot.py` — добавьте в самый конец файла (перед `if __name__ == "__main__":`):

```python
# Для Render.com - добавляем простой веб-сервер для health checks
from http.server import HTTPServer, BaseHTTPRequestHandler
import threading

class HealthCheckHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(200)
        self.send_header('Content-type', 'text/plain')
        self.end_headers()
        self.wfile.write(b'Bot is running')
    
    def log_message(self, format, *args):
        pass  # Отключаем логи health checks

def start_health_server():
    port = int(os.getenv('PORT', 8080))
    server = HTTPServer(('0.0.0.0', port), HealthCheckHandler)
    thread = threading.Thread(target=server.serve_forever)
    thread.daemon = True
    thread.start()
    logger.info(f"Health check server started on port {port}")
```

И в методе `run()` класса `HRAssistantBot` добавьте перед `application.run_polling()`:

```python
def run(self):
    """Запустить бота"""
    # Запускаем health check сервер для облачных платформ
    start_health_server()
    
    # ... остальной код ...
```

#### Шаг 2: Загрузка на GitHub

```bash
# Инициализируйте Git репозиторий
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и привяжите
git remote add origin https://github.com/your-username/hr-assistant-bot.git
git branch -M main
git push -u origin main
```

**ВАЖНО:** Убедитесь, что `.env` добавлен в `.gitignore` и НЕ загружен на GitHub!

#### Шаг 3: Деплой на Render

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройте:
   - **Name:** `hr-assistant-bot`
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python hr_assistant_bot.py`
5. Добавьте **Environment Variables:**
   - `TELEGRAM_BOT_TOKEN` = ваш_токен
   - `ANTHROPIC_API_KEY` = ваш_ключ
6. Выберите **Free** план
7. Нажмите **"Create Web Service"**

Готово! Бот будет развернут за 2-3 минуты.

#### Важно про Free план Render:
- Сервис засыпает после 15 минут без активности
- Первое сообщение боту после "сна" займет ~30 секунд (бот просыпается)
- После этого работает мгновенно
- Для большинства случаев это нормально

---

### 2. Railway.app

**Преимущества:**
- ✅ $5 бесплатных кредитов каждый месяц
- ✅ Бот НЕ засыпает
- ✅ Простая настройка
- ⚠️ Требуется привязка карты (списаний не будет при бесплатном использовании)

#### Шаг 1: Подготовка

Убедитесь, что код на GitHub (см. инструкции для Render выше).

#### Шаг 2: Деплой

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект
5. Добавьте переменные окружения:
   - Settings → Variables → **Add Variable**
   - `TELEGRAM_BOT_TOKEN`
   - `ANTHROPIC_API_KEY`
6. Сохраните

Бот автоматически развернется и будет работать 24/7!

**Мониторинг расходов:**
- $5/месяц бесплатно
- Обычно бот расходует ~$2-3 в месяц
- Можно настроить алерты при превышении лимита

---

### 3. Fly.io

**Преимущества:**
- ✅ Бесплатный план
- ✅ Быстрые серверы по всему миру
- ✅ Не засыпает
- ⚠️ Требуется привязка карты
- ⚠️ Более сложная настройка

#### Шаг 1: Установка CLI

```bash
# Windows (PowerShell)
powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"

# macOS
curl -L https://fly.io/install.sh | sh

# Linux
curl -L https://fly.io/install.sh | sh
```

#### Шаг 2: Настройка

```bash
# Войдите в аккаунт
fly auth login

# Создайте приложение
fly launch

# Следуйте инструкциям:
# - App Name: hr-assistant-bot (или любое)
# - Region: выберите ближайший (например, Frankfurt)
# - Postgres database: No
# - Redis: No
```

Отредактируйте созданный `fly.toml`:

```toml
app = "hr-assistant-bot"
primary_region = "fra"

[build]

[env]
  PORT = "8080"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80

  [[services.ports]]
    port = 443

[checks]
  [checks.status]
    port = 8080
    type = "tcp"
    interval = "15s"
    timeout = "10s"
```

#### Шаг 3: Установка секретов

```bash
fly secrets set TELEGRAM_BOT_TOKEN="ваш_токен"
fly secrets set ANTHROPIC_API_KEY="ваш_ключ"
```

#### Шаг 4: Деплой

```bash
fly deploy
```

Готово! Бот работает 24/7.

---

## Платные VPS опции

Если нужна полная гибкость и контроль:

### DigitalOcean ($4-6/месяц)

**Самый дешевый VPS:**

1. Создайте дроплет на [digitalocean.com](https://digitalocean.com)
   - Ubuntu 22.04
   - $4/month (512MB RAM)
2. Подключитесь по SSH
3. Установите зависимости:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip python3-venv git -y

# Клонирование репозитория
git clone https://github.com/your-username/hr-assistant-bot.git
cd hr-assistant-bot

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
nano .env
# Вставьте ваши ключи, сохраните (Ctrl+X, Y, Enter)
```

4. Настройка автозапуска через systemd:

```bash
sudo nano /etc/systemd/system/hr-bot.service
```

Вставьте:

```ini
[Unit]
Description=HR Assistant Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/hr-assistant-bot
Environment="PATH=/root/hr-assistant-bot/venv/bin"
ExecStart=/root/hr-assistant-bot/venv/bin/python hr_assistant_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните и запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable hr-bot
sudo systemctl start hr-bot
sudo systemctl status hr-bot
```

Готово! Бот работает как служба и автоматически перезапускается при сбоях.

### AWS EC2 / Google Cloud / Azure

Аналогично DigitalOcean, но дороже ($8-15/месяц). Подходит для корпоративного использования.

---

## Сравнение вариантов

| Платформа | Цена | Засыпает? | Сложность | Для кого |
|-----------|------|-----------|-----------|----------|
| **Render** | Бесплатно | Да (15 мин) | ⭐ Легко | Тестирование, личное использование |
| **Railway** | $5/мес бесплатно | Нет | ⭐⭐ Легко | Малый бизнес, до 50-100 пользователей |
| **Fly.io** | Бесплатно | Нет | ⭐⭐⭐ Средне | Опытные разработчики |
| **DigitalOcean** | $4-6/мес | Нет | ⭐⭐⭐⭐ Сложно | Полный контроль, масштабирование |
| **AWS/GCP** | $8-15/мес | Нет | ⭐⭐⭐⭐⭐ Очень сложно | Корпорации, высокие нагрузки |

---

## Рекомендации по выбору

### Для начала:
→ **Render.com** — проще всего, бесплатно навсегда

### Для постоянного использования:
→ **Railway.app** — бот не засыпает, $5 бесплатно каждый месяц

### Для масштабирования:
→ **DigitalOcean VPS** — полный контроль, $4-6/мес

### Для корпораций:
→ **AWS/GCP** — максимальная надежность

---

## 🔒 Безопасность

**Важные правила:**

1. ✅ **НИКОГДА** не загружайте `.env` на GitHub
2. ✅ Проверьте `.gitignore` перед `git push`
3. ✅ Используйте переменные окружения на платформе
4. ✅ Регулярно обновляйте зависимости
5. ✅ Мониторьте логи на ошибки

**Проверка перед деплоем:**

```bash
# Убедитесь, что .env не отслеживается
git status

# Должно быть пусто или не должно быть .env в списке
# Если .env в списке — STOP! Добавьте в .gitignore
```

---

## 📊 Мониторинг и логи

### Render / Railway:
- Логи доступны в веб-интерфейсе
- Автоматический перезапуск при сбоях

### Fly.io:
```bash
fly logs
```

### DigitalOcean:
```bash
# Просмотр логов
sudo journalctl -u hr-bot -f

# Проверка статуса
sudo systemctl status hr-bot

# Перезапуск
sudo systemctl restart hr-bot
```

---

## 🆘 Troubleshooting

### Бот не отвечает после деплоя:

1. **Проверьте логи** на платформе
2. **Проверьте переменные окружения** — правильно ли установлены?
3. **Проверьте токены** — скопированы полностью?
4. **Попробуйте** `/new` в боте для сброса состояния

### Бот часто падает:

- Проверьте баланс на Anthropic API
- Увеличьте память (если используете VPS)
- Проверьте логи на ошибки

### Бот работает медленно:

- Это нормально для Claude (10-30 сек на ответ)
- Проверьте регион сервера (чем ближе к вам, тем лучше)

---

## 💡 Оптимизация расходов

### Для Anthropic API:
- История хранит только 20 последних сообщений
- Используйте `/new` для очистки истории
- Ожидаемый расход: $3-5 на 1000 сообщений

### Для Railway:
- $5 кредитов хватает на ~2000-3000 сообщений/месяц
- Настройте алерты в Settings → Usage

---

## ✅ Чеклист деплоя

- [ ] Код загружен на GitHub
- [ ] `.env` в `.gitignore`
- [ ] Выбрана платформа хостинга
- [ ] Создан аккаунт на платформе
- [ ] Репозиторий подключен
- [ ] Переменные окружения установлены
- [ ] Бот задеплоен
- [ ] Бот отвечает в Telegram
- [ ] Логи проверены — нет ошибок

---

## 🎯 Что дальше?

После успешного деплоя:

1. **Протестируйте бота** с разными запросами
2. **Настройте мониторинг** (проверяйте логи раз в неделю)
3. **Соберите обратную связь** от пользователей
4. **Масштабируйте** при необходимости

---

**Готово! Ваш бот теперь работает 24/7 и доступен всем пользователям! 🚀**
